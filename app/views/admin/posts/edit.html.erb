<h1>Edit <%= @category.name %></h1>

<%= form_for @category, url: admin_post_path(@category), method: :patch do |f| %>
  <%= f.label :name%>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', admin_posts_path %>